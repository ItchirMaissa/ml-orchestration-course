name: CD

on:
  push:
    branches: ["main"]
    tags: ["v*.*.*"]

concurrency:
  group: cd-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  build-push:
    name: Build & push de l'image API (GHCR)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout du depot
        uses: actions/checkout@v4

      # Les étapes Docker sont commentées en attendant la séance Docker

      # - name: Mise en place de Buildx
      #   uses: docker/setup-buildx-action@v3

      # - name: Connexion a GHCR
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}

      # - name: Calcul des tags et labels
      #   id: meta
      #   uses: docker/metadata-action@v5
      #   with:
      #     images: ghcr.io/${{ github.repository }}/mlproject-api
      #     tags: |
      #       type=raw,value=latest,enable={{is_default_branch}}
      #       type=sha
      #       type=semver,pattern={{version}}

      # - name: Build & push de l'image
      #   uses: docker/build-push-action@v5
      #   with:
      #     file: docker/Dockerfile.api
      #     push: true
      #     tags: ${{ steps.meta.outputs.tags }}
      #     labels: ${{ steps.meta.outputs.labels }}
      #     cache-from: type=gha
      #     cache-to: type=gha,mode=max

      - name: CD en attente de la séance Docker
        run: echo "Pipeline CD prêt - étapes Docker à activer après la séance Docker"
